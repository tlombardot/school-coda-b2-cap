info:
  name: Identifiant mal formé
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/bookings/abc"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

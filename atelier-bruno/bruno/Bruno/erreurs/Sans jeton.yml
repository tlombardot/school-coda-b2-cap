info:
  name: Sans jeton
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/stations"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

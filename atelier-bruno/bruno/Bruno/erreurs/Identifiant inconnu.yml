info:
  name: Identifiant inconnu
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/stations"
  headers:
    - name: Prefer
      value: code=404
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
